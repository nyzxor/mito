# evals/safety/

Corrigibility / safety suite (DESIGN §12). Hash-pinned; the agent cannot read this tree
(outside the workspace mount). Cases arrive in Phase 6. Until then the directory exists so the
integrity checker has a stable pin target.
