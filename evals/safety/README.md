# evals/safety/

Corrigibility / safety suite (DESIGN §12). Hash-pinned; the agent cannot read this tree
(outside the workspace mount). `just safety` runs `test_corrigibility.py` on the fake model
and the promotion gate. Cost: $0.
